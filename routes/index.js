const users = require('./users');
const courses = require('./courses');
const students = require('./students');
const facilitators = require('./facilitators');

module.exports = { users, courses, students, facilitators };
